#
# .bashrc file for jalal
#


function elite
{

local GRAY="\[\033[1;30m\]"
local LIGHT_GRAY="\[\033[0;37m\]"
local CYAN="\[\033[0;36m\]"
local LIGHT_CYAN="\[\033[1;36m\]"
local NO_COLOUR="\[\033[0m\]"

case $TERM in
    xterm*|rxvt*)
        local TITLEBAR='\[\033]0;\u@\h:\w\007\]'
        ;;
    *)
        local TITLEBAR=""
        ;;
esac

local temp=$(tty)
local GRAD1=${temp:5}
PS1="$TITLEBAR\
$GRAY-$CYAN-$LIGHT_CYAN(\
$CYAN\u$GRAY@$CYAN\h\
$LIGHT_CYAN)$CYAN-$LIGHT_CYAN(\
$CYAN\#$GRAY/$CYAN$GRAD1\
$LIGHT_CYAN)$CYAN-$LIGHT_CYAN(\
$CYAN\$(date +%H%M)$GRAY/$CYAN\$(date +%d-%b-%y)\
$LIGHT_CYAN)$CYAN-$GRAY-\
$LIGHT_GRAY\n\
$GRAY-$CYAN-$LIGHT_CYAN(\
$CYAN\$$GRAY:$CYAN\w\
$LIGHT_CYAN)$CYAN-$GRAY-$LIGHT_GRAY "
PS2="$LIGHT_CYAN-$CYAN-$GRAY-$NO_COLOUR "
}

# There are 3 different types of shells in bash: the login shell, normal shell
# and interactive shell. Login shells read ~/.profile and interactive shells
# read ~/.bashrc; in our setup, /etc/profile sources ~/.bashrc - thus all
# settings made here will also take effect in a login shell.
#
# NOTE: It is recommended to make language settings in ~/.profile rather than
# here, since multilingual X sessions would not work properly if LANG is over-
# ridden in every subshell.

# This might be helpful for Linux newbies who previously used DOS...
# test -f /etc/profile.dos && . /etc/profile.dos

# Some applications read the EDITOR variable to determine your favourite text
# editor. So uncomment the line below and enter the editor of your choice :-)
export EDITOR=/usr/bin/vim
#export EDITOR=/usr/bin/mcedit

# For some news readers it makes sense to specify the NEWSSERVER variable here
#export NEWSSERVER=your.news.server

# this stops grep from scanning .svn directories
export GREP_OPTIONS='--exclude=\*/\.svn/\*' 

PS1="\[\033[1;34m\][\!][\w]\[\033[0m\]\n\u@\h \$ "
#PS1="[\!][\w]\[\033[0m\]\n\u@\h \$ "
export PS1

# try starting keychain:
#keychain id_rsa 2&> /dev/null
#ource ~/.keychain/supergnome-sh

test -s ~/.alias && . ~/.alias

test -s ~/.funcs && . ~/.funcs 

test -s /etc/bash_completion.d/mercurial && . /etc/bash_completion.d/mercurial

#NTA=$HOME/apps/nta/current
#export PATH=$NTA/bin:$PATH
#export PYTHONPATH=$NTA/lib/python2.4/site-packages:$PYTHONPATH

# add the Android SDK to the path
ASDK=$HOME/Projects/android-sdk-linux/
export PATH=$ASDK/tools:$ASDK/platform-tools:$PATH

# Groovy and Grails
export GROOVY_HOME=/home/jalal/apps/groovy
export GRAILS_HOME=/home/jalal/apps/grails

if [ "$PS1" ]; then
  # Disable that damn CAPSLOCK key
  xmodmap -e "remove lock = Caps_Lock"
  xmodmap -e "keycode 51 = numbersign asciitilde numbersign bar bar"
fi

